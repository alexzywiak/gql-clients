type Query {
  posts: [Post]
  post(id: ID): Post
  authors: [Author]
  author(id: ID): Author
}

type Post {
  id: ID
  author: Author
  body: String
}

type Author {
  id: ID
  posts: [Post]
  firstName: String
  lastName: String
}
